spring:
  application:
    name: gateway
  cloud:
    bootstrap:
      enabled: true
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: admin
      username: user

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false